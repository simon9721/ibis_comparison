# Xyce transistor-level reference benches

This folder mirrors the working `ngspice_refspice` transistor-level reference
benches with Xyce-compatible output directives and channel syntax.

The first target is a 1000 ns PRBS7 run through the same new 50-ohm RLGC
channel used by the accepted ngspice pybis pass case.
